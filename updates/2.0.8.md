# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Fixed a miss-typed value that could cause bugs. 
2. Fixed all discord webhooks urls related to tpz_core which was the only script that had left all webhooks inside config which should not be there but in server/discord/webhooks.lua instead. Please, delete all your discord urls for webhooks on config for safety reason and insert them to the mentioned file!
3. Removed 0x0x0x XOR hex protection which was no longer used. 

## Modified Files:

- server/**.lua (Replace all server files for safety).
- config.lua (removed all webhook urls).
- fxmanifest.lua

## Notes

- `THERE ARE NO NOTES.`
